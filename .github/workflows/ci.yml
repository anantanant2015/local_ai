name: CI Pipeline

on:
  push:
    branches: [ master, develop, feature/** ]
  pull_request:
    branches: [ master, develop ]

jobs:
  lint-and-validate:
    runs-on: ubuntu-latest
    name: Lint and Validate

    steps:
      - uses: actions/checkout@v4

      - name: Check Docker files
        run: |
          echo "✅ Validating docker-compose.yml..."
          docker compose config -q

      - name: Validate shell scripts
        run: |
          echo "✅ Checking shell scripts..."
          for script in scripts/*.sh; do
            echo "Checking $script..."
            bash -n "$script"
          done

      - name: Validate JSON files
        run: |
          echo "✅ Validating JSON files..."
          python3 -m json.tool scripts/models.json > /dev/null
          python3 -m json.tool continue_config.json > /dev/null

      - name: Check Makefile syntax
        run: |
          echo "✅ Validating Makefile..."
          make help > /dev/null

  markdown-lint:
    runs-on: ubuntu-latest
    name: Markdown Linting

    steps:
      - uses: actions/checkout@v4

      - uses: nosborn/github-action-markdown-cli@v3.3.0
        with:
          files: README.md
          config_file: .markdownlint.json

  test-setup:
    runs-on: ubuntu-latest
    name: Test Setup Scripts
    services:
      docker:
        image: docker:dind

    steps:
      - uses: actions/checkout@v4

      - name: Run docker-compose validation
        run: |
          echo "✅ Testing docker-compose configuration..."
          docker compose -f docker-compose.yml config

      - name: Test script syntax
        run: |
          bash -n scripts/setup_ollama_docker.sh
          bash -n scripts/start_dev_environment.sh
          bash -n scripts/add_model.sh
          bash -n scripts/switch_model.sh
          bash -n scripts/list_models.sh
          bash -n scripts/model_utils.sh

  documentation:
    runs-on: ubuntu-latest
    name: Check Documentation

    steps:
      - uses: actions/checkout@v4

      - name: Verify README exists
        run: |
          if [ -f README.md ]; then
            echo "README.md exists"
          else
            echo "ERROR: README.md not found"
            exit 1
          fi

      - name: Check for required sections
        run: |
          echo "Checking README sections..."
          missing_sections=""
          grep -q "Quick Start" README.md || missing_sections="${missing_sections}Quick Start "
          grep -q "Configuration" README.md || missing_sections="${missing_sections}Configuration "
          grep -q "Troubleshooting" README.md || missing_sections="${missing_sections}Troubleshooting "
          grep -q "Memory Optimization" README.md || missing_sections="${missing_sections}Memory Optimization "

          if [ -n "$missing_sections" ]; then
            echo "ERROR: Missing sections: $missing_sections"
            exit 1
          fi
          echo "All required sections found!"

  docker-build:
    runs-on: ubuntu-latest
    name: Docker Compose Build

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Validate docker-compose pull
        run: |
          echo "✅ Docker image validation..."
          docker pull ollama/ollama:latest 2>&1 | head -20

  security-scan:
    runs-on: ubuntu-latest
    name: Security Scan

    steps:
      - uses: actions/checkout@v4

      - name: Check for secrets (shellcheck)
        run: |
          echo "✅ Scanning for potential security issues..."
          for script in scripts/*.sh; do
            echo "Scanning $script..."
            # Check for hardcoded credentials (basic check)
            grep -n "password\|secret\|token\|key=" "$script" || true
          done

      - name: Verify no API keys in config
        run: |
          echo "✅ Checking configs for exposed secrets..."
          # This is a safety check - configs may have placeholders
          grep -i "gsk_\|sk-proj-" continue_config.json || true
        continue-on-error: true

  summary:
    runs-on: ubuntu-latest
    name: Build Summary
    needs: [lint-and-validate, test-setup, documentation, docker-build]
    if: always()

    steps:
      - name: Fail if a required job failed
        env:
          LINT: ${{ needs.lint-and-validate.result }}
          TEST: ${{ needs.test-setup.result }}
          DOCS: ${{ needs.documentation.result }}
          DOCKER: ${{ needs.docker-build.result }}
        run: |
          echo "lint-and-validate=$LINT"
          echo "test-setup=$TEST"
          echo "documentation=$DOCS"
          echo "docker-build=$DOCKER"
          ok=1
          for r in "$LINT" "$TEST" "$DOCS" "$DOCKER"; do
            if [ "$r" != "success" ]; then
              ok=0
            fi
          done
          if [ "$ok" -ne 1 ]; then
            echo "Required CI jobs did not all succeed."
            echo "CI is compose/script syntax validation, not a model-serve smoke on GitHub runners."
            exit 1
          fi
          echo "Required jobs succeeded (syntax/docs/image pull only)."
